---
interface Props {
  current?: boolean;
  href: string;
}

const {
  current = false,
  href,
} = Astro.props;
---

<a class="csk-navbar-extra" {href} aria-current={current ? 'page' : undefined}>
  <slot />
</a>

